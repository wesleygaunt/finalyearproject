%need an X and a w
offset = 0.9;   % This is the virtual gnd of the mirror.
% w2 = [1,1];
% w3 = [0.25,0.25];
% w4 = [0.25,0.75];
% w5 = [0.50, 0.50];
% w6 = [0.75, 0.75];
% w7 = [0.1,0.1];

y_model = X*w' + b; % ground truth, actual matrix multiplication
y_model = y_model';       %transpose
y_true_false_model = y_model >= 0; %if >=0, set as 1. else set as 0.
res_model = y_true_false_model == y; %test. will compare
accuracy_model = sum(res_model)/200;

y_analog = importfile("H:\FYP\NN Models\Test 4 - 3D Inputs\results_test_4.csv", [2, Inf]); %import the data from a file called results. Autogenerated
y_analog = y_analog(:,2)';    %get rid of first column
y_analog = y_analog - offset;
analog_true_false = y_analog >= 0; %if >=threshold, 1 else 0.
res_analog = y == analog_true_false; %test. will compare

accuracy_analog = sum(res_analog)/200
scatter(y_model, y_analog, 'r');
xlabel("y = X*w + b");
ylabel("Change from 0.9 V / V");
